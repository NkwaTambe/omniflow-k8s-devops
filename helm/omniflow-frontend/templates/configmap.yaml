apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "omniflow-frontend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "omniflow-frontend.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.config | nindent 2 }}
